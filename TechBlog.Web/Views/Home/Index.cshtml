@model List<Article>

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @foreach (var article in Model)
    {
        <h1 style="color: #0c4128">@article.Title</h1>
        <h3 style="color: #842029;">@article.Content</h3>
        
        <br /> <br />
    }
</div>